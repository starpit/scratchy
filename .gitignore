*~

/target/
# crates/cli/tui is its own standalone workspace (excluded from the root one,
# see root Cargo.toml) — its build output lands here, not under /target/.
/crates/cli/tui/target/
# The deeptools bridge work happens in worktrees whose sessions share one
# machine; a private target dir keeps a parallel cargo run from going green on
# someone else's build.
/crates/compiler/deeptools-lowering-target/
**/*.rs.bk
/bench_results/
.claude/worktrees/

DRAFT_SPEC_DECODE_PLAN.md
SPEC_DECODE_REFACTOR_PLAN.md
flamegraph.svg.gz

# Baked SuperDSC bundles: build output, tens of thousands of files.
.audit-cache/

# editor and IDE paraphernalia
.idea
.vscode
*.swp
*.swo
*~
.DS_Store
